name: CI

on:
  push:
    branches: [main, master, "feature/**"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ruff:
    name: Lint (Ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - run: python -m pip install ruff
      - name: Check for errors
        run: python -m ruff check .

  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install test dependencies
        run: python -m pip install -r requirements_test.txt
      - name: Validate translation and manifest JSON
        run: |
          python -m json.tool custom_components/nice_bidiwifi/manifest.json > /dev/null
          python -m json.tool custom_components/nice_bidiwifi/strings.json > /dev/null
          python -m json.tool custom_components/nice_bidiwifi/translations/en.json > /dev/null
          python -m json.tool custom_components/nice_bidiwifi/translations/ca.json > /dev/null
      - name: Run tests
        run: python -m pytest tests/ -v --cov=custom_components/nice_bidiwifi --cov-report=term-missing
      - name: Compile Python files
        run: python -m compileall -q custom_components tests
